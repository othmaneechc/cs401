apiVersion: v1
kind: Service
metadata:
  name: playlist-api-service
  namespace: othmane
spec:
  type: NodePort
  ports:
    - port: 52001
      targetPort: 52001
      nodePort: 32082
  selector:
    app: playlist-api
